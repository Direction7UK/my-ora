name: Security Scan

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  schedule:
    # Run weekly on Monday at 00:00 UTC
    - cron: '0 0 * * 1'

jobs:
  npm-audit:
    name: NPM Security Audit
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        directory: [backend, web]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        working-directory: ./${{ matrix.directory }}
        run: npm ci

      - name: Run npm audit
        working-directory: ./${{ matrix.directory }}
        run: |
          echo "Running npm audit for ${{ matrix.directory }}..."
          set +e  # Don't exit on error
          npm audit --audit-level=moderate
          AUDIT_EXIT_CODE=$?
          if [ $AUDIT_EXIT_CODE -ne 0 ]; then
            echo "⚠️ Security vulnerabilities found (non-blocking)"
            echo "Run 'npm audit fix' locally to fix automatically fixable issues"
          else
            echo "✅ No vulnerabilities found"
          fi
          exit 0  # Always succeed
        continue-on-error: true

  # Uncomment if you want to use Snyk or other security tools
  # snyk-scan:
  #   name: Snyk Security Scan
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Run Snyk to check for vulnerabilities
  #       uses: snyk/actions/node@master
  #       continue-on-error: true
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
  #       with:
  #         args: --severity-threshold=high

